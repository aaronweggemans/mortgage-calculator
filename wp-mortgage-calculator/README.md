# Mortgage calculator

Make sure, before reading this README.md, you have built the web-component using `npm run build`.

You need to this inside the root project.

## Test the created web component locally

Use `npx http-server` to run the web-component locally. 
Then open your browser and go to `localhost` to see the mortgage calculator in action.
